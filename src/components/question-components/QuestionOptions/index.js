import React from 'react';
import PropTypes from 'prop-types'

export const QuestionOptions = ({something}) => {
    return (
        <div >

        </div>
    )
};

QuestionOptions.propTypes = {
    codeString: PropTypes.string
};